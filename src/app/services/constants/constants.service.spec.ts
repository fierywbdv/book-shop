import { TestBed } from '@angular/core/testing';

import { ConstantsService } from './constants.service';

describe('ConstantsService', () => {
  const service = ConstantsService;

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
