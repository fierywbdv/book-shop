import { OnHoverColorizerDirective } from './on-hover-colorizer.directive';

describe('OnHoverColorizerDirective', () => {
  it('should create an instance', () => {
    const directive = new OnHoverColorizerDirective(undefined, undefined);

    expect(directive).toBeTruthy();
  });
});
