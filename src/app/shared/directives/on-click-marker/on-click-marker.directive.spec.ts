import { OnClickMarkerDirective } from './on-click-marker.directive';

describe('OnClickMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new OnClickMarkerDirective();

    expect(directive).toBeTruthy();
  });
});
